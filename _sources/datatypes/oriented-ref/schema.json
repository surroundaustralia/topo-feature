{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "An oriented reference to a topological object, combining a reference identifier with an orientation indicator ('+' for forward/positive, '-' for reverse/negative). Used in Ring members (referencing edges) and Shell members (referencing faces).",
  "type": "object",
  "properties": {
    "ref": {
       "$ref": "bblocks://ogc.ogc-utils.iri-or-curie",
      "description": "Identifier of the referenced topological object (e.g. a uuid: URI or local id)"
    },
    "orientation": {
      "type": "string",
      "enum": ["+", "-"],
      "description": "Orientation of the reference: '+' for forward/positive direction, '-' for reverse/negative direction"
    }
  },
  "required": ["ref", "orientation"]
}
